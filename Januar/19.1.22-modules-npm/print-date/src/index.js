import moment from "moment"

console.log(moment().format("dd DD.MM"))  // npm install moment

const element = document.createElement("div")
element.innerText = "Hi from JS"
document.body.append(element)

